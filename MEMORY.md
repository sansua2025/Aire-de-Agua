# MEMORY — Builder (AdeA)

## AIR-203 (RLS PII direcciones web, mig 143)
- **Objetos `direcciones_web_geocoded`/`direcciones_web_municipio`/`v_direcciones_web_clean` NO están en migraciones git** (creados directo en PROD fuera del versionado). Un preview branch NUNCA los trae → para validar hay que scaffold-earlos (extraje columnas via information_schema + `pg_get_viewdef(...,true)` de PROD; la vista usa `unaccent` → `CREATE EXTENSION IF NOT EXISTS unaccent` en el branch).
- **Fix advisory 3 ERRORs**: `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` (x2) + `ALTER VIEW ... SET (security_invoker = true)` + `REVOKE SELECT ... FROM anon, authenticated` (solo la vista). Patrón mig 006/059. RLS sin policy = deny-by-default; los 2 tables pasan de ERROR `rls_disabled_in_public` a INFO `rls_enabled_no_policy` (esperado, no bloquea). service_role bypasea RLS → análisis intacto. NO se revocan grants CRUD de tabla (neutralizados por RLS).
- **Validado en branch `air-235-validate` (`jarkvjnwgaqesanuykzh`, ACTIVE_HEALTHY)**: scaffold estado-problema → apply_migration 143 → AC ok (relrowsecurity=true x2, reloptions=security_invoker=true, vista sin SELECT anon/auth y service_role conserva, advisors sin los 3 findings) → drop scaffolding. PROD (`vnctmzsgemefgbtjctlo`) solo lectura para baseline (los 3 ERRORs vivos confirmados). human-gate: apply a PROD lo hace el humano.

## AIR-259 (Loop v3) — bandas unificadas en percentiles (mig 141)
- **En ESTA sesión builder SÍ tuve Supabase MCP tools** (deferred): via `ToolSearch "select:mcp__f0e900e4-...__list_migrations,apply_migration,create_branch,execute_sql,get_advisors"`. Contradice notas viejas ("MCP no expuesto en builder"). Los tools están deferred → hay que fetchear el schema con ToolSearch antes de llamarlos. El prefijo del server Supabase esta sesión fue `f0e900e4-dab4-4a99-ae15-05fb4354b0df`.
- **`guard-prod-writes.sh` NO intercepta** los nombres prefijados `mcp__f0e900e4-...__apply_migration`/`execute_sql` (solo matchea `mcp__supabase__*` literal). Disciplina AIR-162 a mano: preview branch para DDL, PROD solo lectura.
- **Diseño AIR-259**: fuente única `analytics.bandas_percentiles(p_semana_inicio date)` = percentiles p25/mediana/p75 + fence de Tukey `low=p25-k·IQR / high=p75+k·IQR`, `k=brand_config.umbrales.banda_iqr_k` (=1.5, NO hardcode). La consumen detección (`evaluate_detectors` ramas cvr/aov) y narración (`get_series_contexto` bandas_8w). `banda_desviaciones` (k viejo de media±k·stddev) queda DEPRECADO en umbrales, no se borra.
- **Ventana de la banda (sutileza money-adjacent)**: bandas_percentiles usa "8 semanas más recientes con semana_inicio<p, LUEGO drop nulls por métrica" — que es la ventana que `get_series_contexto` (mig 139) YA usaba. El detector VIEJO (mig 134) usaba "8 valores no-nulos de esa métrica" (per-metric). Al unificar en la ventana de la NARRACIÓN, el conteo old-vs-new de cvr cambió de la estimación del analyst (~6) a 7 real (cvr_web tiene semanas null → las dos ventanas difieren). aov exacto 7→6 (aov sin nulls). Documentado para el owner; la implementación es la correcta (byte-idéntico narración es requisito duro).
- **Byte-idéntico get_series_contexto**: bandas_percentiles redondea por métrica IGUAL que mig 139 (cvr 6 dec, aov/ventas 2 dec); bandas_8w proyecta `bp->'metrica'->'p25'` (jsonb number extraído) == `round(...,6/2)` directo → jsonb idéntico. Probado en branch: `bandas_percentiles(ult)` p25/p75/mediana == lo que emite get_series_contexto para el mismo anchor (fuente_unica_ok=true). El fence low/high vive SOLO en bandas_percentiles, no se proyecta al display (bandas_8w queda con 3 llaves).
- **Branch de validación reutilizable**: `air-235-validate` (project_ref `jarkvjnwgaqesanuykzh`), MIGRATIONS_FAILED pero ACTIVE_HEALTHY. Ya trae bastante scaffolding de AIR-235: analytics schema, brand_config (umbrales `{}`), weekly_snapshot (SIN roas_margen_atribuido/margen_paid_atribuido/revenue_paid_atribuido/mix_canal_web), ventas/venta_items (total_linea/margen_linea GENERATED, refs descuento/cogs_unitario), amplitude, meta_ads_performance, evaluate_detectors/get_series_contexto. FALTABAN: `insight_detectors` (tabla+seed 8), `analytics.get_revenue(date,date,uuid)` (¡3 args, uuid con default!), umbrales de detectores, `ai_analysis_log` tipo-check con `detector_eval`, `ventas.subtotal/total DEFAULT 0` (en PROD son NOT NULL DEFAULT 0; el branch stale no tenía el default → el selftest de get_series_contexto que omite subtotal/total fallaba). Scaffolding PROD-fiel de ese delta + apply_migration del deliverable.
- **check-docstring-rpc-loop out-of-scope**: solo escanea .sql que mencionen `score_confianza` o definan `close_insight_loop|upsert_insight`. Mig de bandas no los menciona → CHECKED=0, exit 0. Aún así evité decimales precedidos de operador (+/-/*) en el header (el `1.5`/`0.25` van tras `default `/`ARRAY[` = narrativos). El check tampoco matchea `AS $fn$` (busca `AS $$` literal) → si estuviera en alcance daría WARN, no FAIL.
- Selftest evaluate_detectors: fixture de banda CONSTANTE (IQR=0) NO distingue stddev de IQR (fence colapsa a la constante) → añadí escenario B (8 semanas dispersas, año 2997 para no chocar con el escenario A en 2999 vía la ventana `semana_inicio<p ORDER BY DESC LIMIT 8`): valor ENTRE p75 y p75+k·IQR NO dispara (prueba la expansión k·IQR), valor > high SÍ dispara, valor_referencia=mediana. weekly_snapshot UNIQUE(semana_inicio) → años distintos por escenario.

## AIR-239 (Loop v3 F1-b) — LLM narra sobre hechos (wrapper 138 + E5A HECHOS)
- **Wrapper PostgREST trivial:** `public.analytics_evaluate_detectors(date,date)` = passthrough `SELECT analytics.evaluate_detectors(...)`, byte-idéntico al patrón mig 030 (`SECURITY DEFINER SET search_path = public, analytics`, `REVOKE ALL FROM PUBLIC, anon, authenticated` + `GRANT service_role`). Los wrappers `public.analytics_*` de PROD tienen proconfig real `search_path=public, analytics` (verificado). Advisors: 0 findings sobre el wrapper (search_path fijo → no `function_search_path_mutable`; anon/auth revocados → no `anon/authenticated_security_definer_function_executable`, que solo pegan a legacy `backfill_orders`/`update_ventas_utm_from_amplitude`).
- **`analytics.evaluate_detectors` devuelve un ARRAY jsonb bare** (no objeto). PostgREST lo entrega tal cual; n8n httpRequest puede (a) partir el array en N items o (b) dar 1 item cuyo json ES el array. Normalizar: `const arr=$('..').all().map(i=>i.json); let a=(arr.length===1&&Array.isArray(arr[0]))?arr[0]:arr;`. Campos del hecho: `insight_key,dominio,tipo,disparado,valor,referencia,muestra_n,muestra_suficiente,impacto_cop,metrica_clave,signo_esperado`, opcional `ad_id`, opcional `error`(=SQLERRM, ÚNICO texto libre → sanear/excluir). En el prompt renombro `valor→valor_observado`, `referencia→valor_referencia` para que el LLM copie 1:1 y el validador compare por mismo nombre.
- **Validador post-parse** (fuente canónica `dashboard/evals/cerebro/validate-insights.ts`, ESPEJO en jsCode de `Parse Claude`): acepta insight si su `insight_key` matchea un hecho con `disparado===true && muestra_suficiente===true` y `valor_observado/valor_referencia/metrica_clave` IGUALES (numEq null-safe); o `hipotesis_*` con `score<=0.5`, máx 1. Taggea `fuente` ('detector'/'hipotesis'). Test `validate-insights.test.ts` (7 casos, pure, corre siempre sin DB). Los code nodes n8n NO pueden `import` → mirror manual (comenté el pareo en ambos).
- **check-data-rules R1 `valor_compras` FALSO POSITIVO al editar un nodo jsCode de una-línea** (render_email). El fix AIR-143 (diff-mode solo líneas añadidas) NO ayuda si TODO el jsCode es UNA línea JSON: editar cualquier parte re-añade la línea entera con el `valor_compras=$0` pedagógico preexistente → R1 pega en `--diff`. Solución aplicada: renombré el token del pixel-alert a `<code>value=$0</code> (bug historico AIR-71)` — mismo significado, sin el literal `valor_compras`. (CLAUDE.md ya nombra el bug como `value=0`/AIR-71.)
- **E5A git export NO trae `activeVersion`** (grafo único `nodes`/`connections`) — DISTINTO del workflow VIVO `9uDRQuIEOjKwRfYF` que SÍ tiene `activeVersion` (copia de 22 nodos, `versionId==activeVersionId`, active=true). El check `check-n8n-graph-parity.sh` opera sobre el JSON de git → SKIP (N/A) porque no hay activeVersion allí → editar SOLO `nodes` es correcto. Al importar el JSON, n8n regenera la versión activa desde `nodes` (sin staleness AIR-140 en el artefacto). Nodo nuevo cableado `RPC get_brand_config → RPC evaluate_detectors → Build Prompt`.
- **`validate_workflow` (n8n MCP) valida SOLO código SDK TypeScript, NO el JSON export nativo.** Para editar un export nativo existente es N/A; validación equivalente = JSON well-formed + `vm.Script('(function(){'+jsCode+'})')` por nodo + chequeo estructural (cada `$('Node')` y cada connection resuelve a un nodo existente, sin ids/nombres duplicados).
- **Validar wrapper en preview branch stale (replay roto):** reusé un branch efímero existente (ACTIVE_HEALTHY, MIGRATIONS_FAILED). Scaffolding mínimo = `CREATE SCHEMA analytics` + STUB `analytics.evaluate_detectors(date,date) returns jsonb ... AS $$ select '[]'::jsonb $$` (LANGUAGE sql valida el cuerpo del passthrough al CREATE, basta la firma) + `brand_config` mínima para probar el append de persona. service_role/anon existen siempre (roles globales). `apply_migration` del wrapper + `execute_sql` del UPDATE persona.
- **guard-prod-writes.sh matchea SOLO `mcp__supabase__*` literal**, NO los nombres prefijados `mcp__f0e900e4-...__apply_migration`/`execute_sql` de esta sesión → el hook NO los intercepta (cae a `*) exit 0`). No relajarse: disciplina AIR-162 a mano (PROD solo lectura; DDL en branch).
- **Persona DRAFT = human-gate:** el `UPDATE brand_config SET persona_system = persona_system || E'\n\n' || $q$...$q$` va COMENTADO en la migración (aplicar 138 crea SOLO el wrapper). El humano lo corre a PROD al aprobar. Dollar-quote `$persona_draft$` para tildes/comillas/`hipotesis_`. Validé el append en branch (40→991 chars). check-docstring-rpc-loop: mig entra en alcance por mencionar `score_confianza` → mantener el header (comentarios `--` antes del 1er `AS $$`) SIN literales decimales; el `<= 0.5` vive en el string del UPDATE (post-`AS $$` = "cuerpo") → no lo escanea.

## Dashboard (`/home/user/Aire-de-Agua/dashboard`)
- Next.js 16 NO estándar (Turbopack). `node_modules` no viene instalado en el contenedor cloud: `npm install` antes de typecheck/build (registry funcionó, ~16s).
- Verificación: `npm run typecheck && npm run build && npm run lint && npm run test`. Lint sale exit 0 con warnings pre-existentes (topbar/tooltip + el patrón `useEffect(() => setState(initialProp), [initialProp])` de InsightsCard — warning `react-hooks/set-state-in-effect`, aceptado, no es error).
- **Dos clientes Supabase**:
  - Lectura: `lib/supabase/server.ts` → rol **anon** (publishable key), scopeado a schema `analytics`. Tipos manuales en `types/analytics.ts` (NO genera `gen types`). Toda vista nueva del dashboard necesita `GRANT SELECT ... TO anon` además de `dashboard_reader`.
  - Escritura: `lib/supabase/admin.ts` `getAdminClient()` → **service_role**, scopeado a schema **public**. Por eso los RPC que invoca (`analytics_aprobar_propuesta`, `analytics_marcar_estado_insight(s)`, y ahora `analytics_aprobar_learning`) viven en `public` con prefijo `analytics_*`. Tipos en `types/database.ts` (Functions) — se editan a mano al añadir RPC (no están en migraciones).
- Write-path HITL: route handlers en `app/api/propuestas/*` → `auth()` → `getAdminClient().rpc(...)` → `revalidateTag('insights', { expire: 0 })`. Patrón a imitar: `aprobar/route.ts`.
- Anti prompt-injection en render: `sanitizeText()` (remueve control chars, colapsa espacios; React además escapa). Copia local en `app/(dashboard)/ai/page.tsx` y `app/(dashboard)/page.tsx` (este último es AIR-128, NO tocar). Sanear textos de DB en el servidor antes de pasarlos al client component.
- Componente capas `/ai`: `components/ai/ai-charts.tsx`. `bucketize()` reparte insights; `TriageView` dibuja Capa 1 (cola), Pospuestos, Historial, Contexto. Capa 2 (AIR-61) = `LearningCard` para strategic_learnings candidatos (sección "Esperando tu aprobación", acento warning).

## Supabase migraciones
- Numeración secuencial estricta: `ls supabase/migrations/ | grep -oE '^[0-9]+' | sort -n | tail -1`. Último al cerrar AIR-61: **066**. Al cerrar AIR-241: **136**.
- **Preview branches de este proyecto NO clonan el schema de PROD**: el replay de migraciones git falla temprano (última aplicada en el branch nuevo: `20260428012514`, mig ~inicial) → `MIGRATIONS_FAILED`/`ACTIVE_HEALTHY`. `decisiones`/`brand_config`/reader-roles/columnas nuevas de insights (insight_key, signo_predicho, estado_accion) AUSENTES. Para validar una mig nueva hay que **construir scaffolding PROD-fiel** vía `execute_sql` (recrear roles anon/service_role/dashboard_reader/el_cerebro_reader, schema analytics, brand_config + seed AdeA, ALTER insights ADD COLUMN, decisiones con delta_real_pct GENERATED) y LUEGO `apply_migration`. Verificado en AIR-241.
- **Drift git↔PROD (pre-flight AIR-162 §4)**: al cerrar AIR-241, PROD `list_migrations` estaba en `134_air238`; git main ya tenía `135_air240` (mergeado, deploy a PROD pendiente). Una mig nueva puede depender de columnas de una mig git aún no desplegada — confirmar independencia (136 solo LEE decisiones/insights, no depende del trigger de 135).
- **Vista SECURITY INVOKER correcta**: `CREATE VIEW x WITH (security_invoker = true) AS ...`. get_advisors NO la marca como `security_definer_view` (a diferencia del default de views, que SÍ se marca). Para exponerla a anon sin darle SELECT en tablas base, el par es: vista INVOKER + RPC `SECURITY DEFINER SET search_path` que hace `SELECT ... FROM la_vista` (el owner postgres del RPC es el invoker efectivo de la vista → lee las base). Patrón usado en AIR-241 (`analytics.v_detector_hit_rate` + `get_detector_hit_rate()`).
- **hit_rate calibrado (AIR-241)**: `aciertos::numeric / nullif(aciertos+fallos,0)` → NULL sin señal (nunca 0). acierto/fallo direccional vs `insights.signo_predicho` ∈{sube,baja,NULL}; `|delta_real_pct| < umbral` = sin_cambio (fuera del denominador); signo NULL = sin_prediccion (aparte). Umbral en `brand_config.umbrales->hit_rate_ruido_pct` (seed idempotente `jsonb_build_object(k,v) || umbrales` deja ganar a umbrales → no clobbera tuning). El join es `decisiones.insight_id → insights.id` agrupado por `insights.insight_key` (decisiones NO tiene insight_key propio).
- **Selftest determinista sin residuo** (patrón migs 134/135/136): función `SECURITY DEFINER` que siembra fixtures dentro de `BEGIN...RAISE EXCEPTION '..._ROLLBACK'; EXCEPTION WHEN OTHERS THEN IF SQLERRM NOT LIKE '%_ROLLBACK%' THEN RAISE; END IF; END;` → subtransacción SIEMPRE revertida (cero residuo, sin DELETE). Retorna jsonb `.ok`. REVOKE ALL FROM PUBLIC,anon,authenticated + GRANT EXECUTE service_role. El eval `dashboard/evals/cerebro/*.test.ts` lo invoca vía `callRpc(...)` (molde `detectors-eval.test.ts`; SKIP sin SUPABASE_SERVICE_ROLE_KEY).
- Numeración secuencial estricta (repetido): último **136** al cerrar AIR-241.
- Vistas del schema `analytics` = SECURITY DEFINER por default → permiten a anon/dashboard_reader leer derivados sin grant en tabla base. get_advisors reporta `security_definer_view` (intencional, precedente `v_loop_system_health` AIR-87). NO pasar a security_invoker esas vistas dashboard.
- `strategic_learnings` (mig 058): `score_estabilidad` y `margen_*` son GENERATED STORED. estado ∈ candidato/en_revision/aprobado/promovido/rechazado/deprecado. anon/authenticated sin acceso a la tabla base.
- Hardening RPC (AIR-86, mig 060): `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated` + `GRANT EXECUTE ... TO service_role`. `ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE EXECUTE ON FUNCTIONS FROM anon, authenticated` ya activo.
- **Repo público → cero cifras financieras.** Comentarios SQL de migraciones, bodies de PRs y commits NUNCA embeben montos en pesos (ni totales ni subtotales). Usar: "(montos: ver issue Linear)". Las cifras de conteo (número de filas, rango de fechas) son OK. (AIR-175, candidate R8 en check-data-rules si repite.)
- **Preview branch stale:** `create_branch` snapshottea PROD al momento de creación. Migraciones aplicadas a PROD después del snapshot NO llegan al branch; hay que `apply_migration` manualmente sobre el branch_id.
- **Carga masiva sin psql:** `execute_sql` no acepta ~69KB. Patrón: script local emite chunks idempotentes (50 filas/chunk, ON CONFLICT DO UPDATE), ejecutados en orden vía execute_sql. Validar contra Postgres 16 local primero.

## E5A / búsqueda semántica (AIR-70)
- E5A NO hace bulk select: nodo `Build Prompt (sanitized)` (id `cd185aa9-…`) usa `get_memoria_activa(null,10,10)` + snapshot agregado + anomalías, todo saneado. Input ~3,7K tok (chars/4): memoria ~41%, snapshot ~30%, system ~16%, schema ~8%. vs bulk select* sin truncar ~66K tok solo memoria (~43× ahorro ya capturado por LIMIT 10/10 + sanitize truncado).
- **Firma real `buscar_brand_knowledge(query_embedding vector, limite int, filtro_categoria text)`** — recibe VECTOR 1536, NO `query_text`. Pseudocódigo del issue erróneo. Generar embedding OpenAI `text-embedding-3-small` (1536 dims) ANTES de la RPC. Igual `buscar_productos(vector, int, text, text)`. Ambas: REVOKE PUBLIC/anon/auth + GRANT service_role (mig 007), search_path fijado (mig 061).
- Veredicto AIR-70: NO migrar E5A a semántica (ya mitigado; ranking por confianza/recencia ≠ similitud; ampliaría superficie injection). AIR-67/68/69 no existen → CA bloqueados. Doc: `docs/agentes/AUDITORIA-CONTEXTO-E5A.md`.

## Entorno
- En sesión con worktree aislado (`.claude/worktrees/agent-*`): editar/escribir SIEMPRE la copia del worktree, NO el checkout compartido (`/home/user/Aire-de-Agua/...`) — Write al path compartido falla con error de aislamiento. MEMORY.md y CLAUDE.md del worktree son archivos distintos del checkout: hay que Read la copia del worktree antes de editar.
- Contenedor efímero (otras sesiones): NO worktrees, NO cambiar de rama. Supabase MCP NO disponible como tool en sesión builder (sin CLI ni credenciales) → escribir la migración como artefacto fiel y dejar `create_branch`/`apply_migration`/`get_advisors` al reviewer.
- Contenedor efímero: NO worktrees, NO cambiar de rama. Supabase MCP NO disponible como tool en sesión builder (sin CLI ni credenciales) → escribir la migración como artefacto fiel y dejar `create_branch`/`apply_migration`/`get_advisors` al reviewer.
- **MCP (Supabase y n8n) NO expuestos como tools en sesión builder** aunque sus *instructions* aparezcan en contexto. Solo tengo Read/Write/Edit/Bash/Grep/Glob. `apply_migration`/`get_advisors`/`validate_workflow`/`create_workflow_from_code` los corre el reviewer/orchestrator. Reportarlo explícito al terminar (regla de capacidad).

## AIR-79 (E5-L brand_config) — patrones
- Mig **080**: `public.brand_config` (marca_id PK uuid fijo `a1de0a9a-0000-4000-8000-000000000001`=AdeA, persona_system text, umbrales jsonb, canales jsonb). RLS patrón insights/decisiones (anon/public revocado, contiene prompt). RPC `get_brand_config(p_marca_id)` SECURITY DEFINER, solo service_role (hardening AIR-86). Resuelve TODO(AIR-79) de 058: `marca_id` FK+default+backfill en `decisiones` y `strategic_learnings`.
- **Persona byte-idéntica**: el `const systemPrompt` del nodo "Build Prompt (sanitized)" de E5A tiene una mezcla de `\n` (escape) y UN salto de línea REAL dentro del literal (antes de "REGLA DE signo_predicho"). Para seed fiel: extraer el valor RUNTIME del literal JS (no el JSON crudo) y meterlo dollar-quoted `$persona$...$persona$`. Verificado byte-idéntico (2404 chars).
- **E5A JSON tiene 2 copias del grafo**: `nodes` (live, versión AIR-119 con `snapshotSanitized`) y `activeVersion.nodes` (copia, usa `snapshot` raw). Editar AMBAS + sus `connections`. Refactor: nodo HTTP `RPC get_brand_config` insertado entre `RPC get_memoria_activa` y `Build Prompt (sanitized)`; el jsCode pasa de literal a `const brandCfg = $('RPC get_brand_config').first().json; const systemPrompt = brandCfg.persona_system;`. persona_system → rol **system** (vía `system: $json.system_prompt`), NUNCA dentro de `<data>`. No tocar `sanitize()`/`snapshotSanitized`.
- Validación local sin MCP: `node vm.Script('(function(){'+jsCode+'})')` para syntax-check (el `return` top-level da "Illegal return" — normal en n8n, envolver en función).

## El Cerebro — RPCs gobernadas analytics.* (AIR-65/151-156)
- **Bug histórico get_roas (AIR-65, mig 088)**: `analytics.get_roas` sumaba `v_paid_performance_diario WHERE fecha BETWEEN` → ancla revenue↔fecha; con conversión diferida (~50%) subcuenta ~2x. FIX: agregar POR ADSET sobre ventana completa — CTE `gasto_adset`(meta_ads_performance) FULL OUTER JOIN CTE `rev_adset`(vista_atribucion_web_con_margen, canal_tipo='paid'). **get_roas NO filtra cobertura_cogs** (devuelve revenue, no margen; revenue_venta existe siempre — filtrar perdería ventas). Mayo-2026: bug 1.741.200/11/0.69x → correcto 3.716.968/22/1.4789x (=1:1 con get_web_attribution paid). `v_paid_performance_diario` SOLO para tendencia diaria por adset (ADR-001/002). Firma `get_roas(date,date,text)` argCount:3 INMUTABLE (reader.ts/tools.ts/database.ts).
- **golden_queries (mig 084)**: tabla append-only `public.golden_queries`, col `activo boolean DEFAULT true`. El harness (`evals/cerebro/client.ts goldenByTool`) filtra `activo=true` y toma la 1ª fila por `tool_call->>'tool'`. Para invalidar un seed: `UPDATE ... SET activo=false` la fila vieja (NO borrar — trazabilidad) + INSERT fila nueva con pregunta DISTINTA (pregunta_hash UNIQUE) y `activo=true`. La migración corre como postgres → puede UPDATE aunque el runtime (service_role) sea solo SELECT/INSERT.
- **eval_recompute (mig 086)**: oracle `analytics.eval_recompute(p_task_id,p_variant)` plpgsql, despachador whitelisted por taskId (sin SQL dinámico), espeja 1:1 las cadenas `recompute_sql_*` de `dashboard/evals/cerebro/tasks.json`. Al añadir/cambiar un task: editar AMBOS (oracle + tasks.json) en paralelo. EXECUTE solo service_role.
- **evals tests**: `reconcile.test.ts` SKIP sin `SUPABASE_SERVICE_ROLE_KEY` (env-guard pasa si `EVALS_REQUIRED!=1`). GATE asserta `results.length===TASKS.length` → cada task nuevo NECESITA su `it()` que llame `record()`. `skill-static.test.ts` corre siempre (no DB): exige `analytics.<rpc>(` exactamente 1 vez c/u en SKILL.md + `toContain('revenue_atribuido')` (mantener mención al describir v_paid_performance_diario) + columnas reales (total_linea/ordered_at/America/Bogota/estado_pago/titulo). Un Prettier hook reformatea reconcile.test.ts entero (`'`→`"`) al editar — diff grande pero inocuo; tsc+vitest validan.
- Verificación builder sin BD: `npm install` (node_modules no viene) + `npm run typecheck` (tsc limpio) + `npx vitest run evals/cerebro/skill-static.test.ts evals/cerebro/reconcile.test.ts` + `jq empty tasks.json` + `bash scripts/agent/check-data-rules.sh --file <archivos>`. Lint rompe en cloud (`eslint-config-next` ausente) — ignorar.
- ADR en `docs/adr/ADR-NNN-*.md` (último ADR-002). ADR-001 §3 dejó "Fase 3b" = este fix por-adset.

## n8n "slim" de payload Shopify → RPC — verificar contrato completo (AIR-43)
Cuando un nodo Code recorta el payload de Shopify a un whitelist de campos antes de mandarlo a un RPC
(`backfill_orders` y similares), el whitelist puede quedar desincronizado del contrato real del RPC
**sin error visible**: el campo simplemente no llega, el RPC hace fallback silencioso (`COALESCE`) y la
columna queda NULL/vacía sin lanzar excepción en n8n ni en Postgres. Caso real: el slim de
`E2_Backfill_Historico_Shopify` mandaba `payment_gateway` (deprecado por Shopify) pero no
`payment_gateway_names` (array, fuente que `backfill_orders` prioriza vía
`COALESCE(ord->'payment_gateway_names'->>0, ord->>'payment_gateway')`) → `metodo_pago` NULL en 96,6% de
las filas. Al editar un slim de payload: leer el `COALESCE`/mapeo de campos del RPC destino y confirmar
que CADA campo que lee está en el whitelist, no solo los "obvios" — especial atención a pares
campo-nuevo/campo-deprecado de la API de origen. 1ª vez que se ve este patrón — si vuelve a repetirse,
candidato a check estático en `check-data-rules.sh` (diff de campos usados en RPC `->>`/`COALESCE` vs.
whitelist del nodo Code que lo alimenta).

## AIR-235 (Loop v3 F0-b) — get_memoria_activa v2 (mig 131) — patrones
- **`CREATE OR REPLACE FUNCTION` BORRA el `SET search_path` fijado por un `ALTER FUNCTION` previo.**
  Si una migración anterior (AIR-93, mig 061 en este caso) fijó el search_path vía
  `ALTER FUNCTION ... SET search_path`, en vez de declararlo en el `CREATE` original, un
  `CREATE OR REPLACE FUNCTION` posterior lo resetea silenciosamente → reaparece el advisor
  `function_search_path_mutable`. Hay que RE-DECLARAR `SET search_path TO '...'` (mismo valor que
  el proconfig real de PROD, verificado antes) dentro del propio `CREATE OR REPLACE`, no asumir que
  sobrevive. Aplicado correcto en mig 131.
- **Contrato de un RPC consumido por n8n: un campo puede salir del RANKING sin salir del OUTPUT.**
  Al reescribir `get_memoria_activa`, `score_confianza` dejó de ordenar (ahora manda recencia) pero
  se preservó en el jsonb porque `Build Prompt`/`Render Email HTML` lo leen. Al tocar la lógica de
  selección/orden de un RPC ya consumido, grep de los campos del jsonb en los consumidores (n8n,
  dashboard) ANTES de quitar uno — "ya no se usa para X" no implica "ya no se usa para Y".

## AIR-234 (Loop v3 F0-a) — auto-resolución por contradicción (mig 130)
- **Preview branch replay ROTO en este proyecto**: `create_branch` deja el branch en `MIGRATIONS_FAILED` (tanto el `main` viejo como uno nuevo). El replay del historial se detiene temprano → snapshot ~abril (57 migr, SIN schema `analytics`, sin `insights.estado_accion`/`insight_key`, sin `set_updated_at`). El `preview_project_status` sí es ACTIVE_HEALTHY → la BD es consultable. `create_branch` NO trae datos (`with_data:false`) → schema-only.
- **Patrón de test con branch stale**: aplicar una migración de *scaffolding* (NO deliverable) que recrea el delta PROD-fiel que tu migración necesita (verifica los valores reales con `execute_sql` read-only sobre PROD ANTES), luego `apply_migration` de la migración real encima. Todo off-PROD (AIR-162 respetado).
- **`ai_analysis_log.estado`** CHECK admite `running/completed/error/ok/abortado/skip_duplicado` → usar `'completed'` (inglés), NO `'completado'`. El `tipo` CHECK viene de 070_air67; ampliar aditivo (DROP+ADD con TODO el ARRAY vigente + el nuevo valor).
- **Grants default de Supabase**: tablas nuevas en `public` reciben ALL para `anon` Y `authenticated`. `REVOKE ... FROM anon, public` NO cubre `authenticated` → revocar los TRES (`anon, authenticated, public`) para blindar una tabla sensible (RLS ON + sin policy + grant solo service_role). service_role tiene BYPASSRLS → los RPC SECURITY DEFINER leen sin policy.
- **Eval SQL self-contained (rollback subtransacción)**: helper `analytics.*_selftest()` que inserta fixtures + corre el RPC real dentro de `BEGIN ... RAISE EXCEPTION 'ROLLBACK' ... EXCEPTION WHEN OTHERS THEN (si SQLERRM no es el rollback intencional → RAISE)`. Las variables plpgsql (veredicto jsonb) SOBREVIVEN al rollback; los writes se revierten → CERO residuo, sin DELETE (respeta "nunca DELETE insights"). Corre seguro contra PROD en CI. El vitest en `dashboard/evals/cerebro/*.test.ts` solo llama al RPC y asserta `.ok` (patrón describeDb skip). Fixtures de insights: `dominio` ∈ CHECK (usa 'general'), `tipo` ∈ CHECK (usa 'patron').
- **[CORREGIDO — el diseño de abajo fue ELIMINADO por seguridad, mismo PR]** El primer diseño de esta migración guardaba `condicion_sql` (texto) en `insight_resolution_rules` y lo corría vía `EXECUTE 'SELECT ('||condicion_sql||')::boolean'`, con guards en runtime (`^select`, rechazo de `;`). Security-review lo bloqueó: evadible (`select evil_writes()`, smuggling multi-columna) → ejecución arbitraria como owner del SECURITY DEFINER. **Diseño final (el que quedó en PROD)**: la tabla YA NO tiene `condicion_sql` — es solo una allowlist (`insight_key` + `descripcion` documental, nunca ejecutable). La condición vive en un **DISPATCHER WHITELISTED** dentro del cuerpo del RPC (`CASE r.insight_key WHEN 'klaviyo_canal_apagado' THEN <query fija> ... ELSE <se salta> END`), mismo patrón que `analytics.eval_recompute` (mig 086). Cero `EXECUTE` de texto almacenado. Regla general: **cualquier `EXECUTE` de una columna de tabla en una función `SECURITY DEFINER` es BLOQUEANTE**, sin importar cuántos guards de runtime tenga — usar dispatcher whitelisted en su lugar. Token literal `'auto-resuelto'` en la nota sigue siendo el contrato con AIR-235, no cambiar.

## AIR-256 (Loop v3) — saneo recursivo por-campo de SERIES en Build Prompt E5A
- **Defensa en profundidad, no `activeVersion`**: el export git de `E5A_Loop_Weekly_Analysis.json` NO tiene `activeVersion` (grafo único `nodes`) → editar SOLO `nodes`; `check-n8n-graph-parity.sh` sale N/A. El publish del workflow vivo (que SÍ tiene `activeVersion`, ambas copias `nodes`↔`activeVersion.nodes`) lo hace el orquestador tras review.
- **Patrón `sanitizeDeep(v)`** añadido junto a `sanitize()` en el nodo `Build Prompt (sanitized)`: string→`sanitize(v)` (default maxLen 200), array→`.map(sanitizeDeep)`, objeto no-null→recorre valores (llaves fijas intactas), else (num/bool/null/undefined)→tal cual. Reemplaza `const seriesSafe = ...? series : {}` por `seriesObj=(...)?series:{}; seriesSafe=sanitizeDeep(seriesObj)`. Antes el jsonb de `get_series_contexto` entraba entero sin saneo por-campo (seguro sólo por invariante remota "RPC emite solo numérico/fechas").
- **Fechas-ISO sobreviven a `sanitize()`**: no hay `<`/`>` ni control chars y son <200 chars → `sanitize` es no-op sobre un ISO/num-string. Por eso `sanitizeDeep` NO necesita casos especiales para fechas; aplicarle `sanitize` las deja idénticas. Un payload solo-numérico queda `deepStrictEqual` al original.
- **Verificación sin MCP**: `node --check` del jsCode envuelto en `(function(){...})` (el `return` top-level de n8n da "Illegal return" si no se envuelve). Test unitario puro de `sanitizeDeep` (string malicioso `<script>evil</script>`→`evil` sin `<`/`>`, `aov:200000` intacto, payload numérico deepEqual). NO tocar HECHOS/SNAPSHOT(AIR-119)/persona/`<data>`/RPC series/validador Parse Claude. Edición byte-a-byte del string jsCode escapado vía script Python con `assert count==1` (Edit tool con `\n` literales es frágil).

## AIR-236 (Loop v3 F0-c) — upsert_insight v3 (mig 132)
- **`analytics.upsert_insight` es SECURITY DEFINER, proconfig real `search_path=public, analytics`** (verificado en PROD). El `CREATE OR REPLACE` DEBE re-declarar `SET search_path TO 'public','analytics'` o reaparece `function_search_path_mutable`. Grants PROD = `postgres`+`service_role` EXECUTE (sin anon/authenticated/PUBLIC); CREATE OR REPLACE los preserva, pero replico el `REVOKE ALL FROM PUBLIC` + `GRANT service_role` de 066 (idempotente). El wrapper `public.analytics_upsert_insight` es un passthrough SQL de firma idéntica → un CREATE OR REPLACE de `analytics.upsert_insight` NO lo toca.
- **v3 cambió la MECÁNICA de match, NO la firma ni el retorno** (`id, accion, score_anterior, score_nuevo, veces_confirmado` intactos → nodo n8n `RPC upsert_insight` del wf `9uDRQuIEOjKwRfYF` no se toca). Match idempotente ÚNICO: `insight_key = p_key AND periodo_inicio IS NOT DISTINCT FROM p_periodo (null-safe) AND vigente=true` ORDER BY ultima_confirmacion DESC LIMIT 1. ELIMINADO el match por `LEFT(titulo,40) ILIKE` (raíz de la mutación silenciosa de la serie AIR-76). Filtrar `vigente=true` = NUNCA resucita una fila auto-resuelta de 234. `insight_key` null → INSERT simple `inserted_sin_key`.
- **Score SIN composición**: `score_nuevo = LEAST(GREATEST(input,0),1)` tal cual, en INSERT y UPDATE (v2 hacía `s+(1-s)*0.15` → confianza de vanidad). Re-run misma semana NO sube. **`veces_confirmado = COUNT(*) del key sobre TODA la historia** (no solo vigentes): count+1 en INSERT, count (incluyéndose) en UPDATE → coherente con `semanas_observado` de 235, no acumulador mutado. Ramas `accion`: inserted / updated_exact / inserted_sin_key.
- **insights CHECKs (PROD, verificados)**: `dominio` ∈ {meta_ads,organico,email,web,producto,cliente,inventario,general,paid,ventas}; `tipo` ∈ {patron,anomalia,correlacion,oportunidad,riesgo,logro}; `estado_accion` NOT NULL default `'pendiente'` ∈ {pendiente,en_curso,hecho,descartado,pospuesto}; `requiere_del_humano` (nullable) ∈ {decidir_urgente,aprobar,informacion,celebrar,nada}; `signo_predicho` ∈ {sube,baja}; `score_confianza` ∈ [0,1]. `insights` NO tiene columnas GENERATED. El INSERT OMITE `estado_accion` (aplica default). `score_confianza` default de columna = 0.8 (pero la función usa fallback propio 0.6 si el payload lo omite — heredado de v2).
- **Preview branch replay SIGUE ROTO** (igual que 234/235): branch nace stale-abril (insights con CHECK viejo de `dominio`, SIN `insight_key`/`signo_predicho`/`estado_accion`/`requiere_del_humano`, SIN schema `analytics`; pgvector SÍ presente). Scaffolding aplicado: `CREATE SCHEMA analytics` + `ALTER TABLE insights ADD COLUMN IF NOT EXISTS` de las 5 faltantes + sus CHECKs PROD-fieles. Los `get_advisors` del branch stale traen MUCHO ruido baseline (`function_search_path_mutable` en funciones `public.*` legacy que en PROD real ya tienen search_path por migraciones que no replayearon, `security_definer_view`, rls_*) — NINGUNO en `analytics` ni sobre mis 2 funciones. Filtrar por "¿el advisor nombra MI objeto?" antes de alarmarse.

## AIR-238 (Loop v3 F1-a) — insight_detectors + evaluate_detectors (mig 134)
- **Dispatcher whitelisted, cero EXECUTE** (mismo patrón 086/130, decisión de seguridad del owner): tabla `public.insight_detectors` = allowlist PURA (`insight_key` PK + dominio/tipo/muestra_minima/metrica_clave/signo_esperado/activo). SIN `condicion_sql`/`formula_impacto_sql`. La lógica de los 8 detectores vive en un `CASE r.insight_key` dentro de `analytics.evaluate_detectors(p_inicio,p_fin)`. Agregar detector = migración que añade rama (trade-off aceptado). RLS ON + REVOKE anon/authenticated/public + GRANT service_role. CHECK dominio/tipo = mismos sets que `public.insights` (compatibilidad F1-b).
- **Gate de muestra: SEÑAL y SUFICIENCIA separadas.** Cada entrada jsonb emite `disparado` (bool) Y `muestra_suficiente = (muestra_n >= muestra_minima)` como campos DISTINTOS. NUNCA suprimir el disparo por muestra chica (CA2: canal 73% con 2 ventas → disparado=true, muestra_suficiente=false). `muestra_minima` vive en la COLUMNA de la tabla (no en SQL). Los UMBRALES (200k/0.3/50k/300/0.8/0.6/k=2/ventana=8) se leen de `brand_config.umbrales` (UPDATE aditivo `umbrales || jsonb_build_object(...)`), NO hardcodeados en el dispatcher (verificar con grep sobre el cuerpo del RPC). Reset de vars scratch (`v_paid_ventas`,`v_top_ventas`) al tope del loop: un `SELECT ... INTO` sin fila deja la var con valor STALE de la iteración previa.
- **Fuentes por detector (todas verificadas en PROD):** klaviyo_canal_apagado←weekly_snapshot.emails_enviados; margen_paid_negativo←roas_margen_atribuido/gasto_meta/margen_paid_atribuido (impacto=margen_paid_atribuido-gasto_meta); roas_real_vs_meta_divergente←roas_meta_atribuido (REAL) vs roas_meta (auto-reporte del pixel, sólo como referencia a marcar poco fiable — NUNCA emitir valor_compras); ad_tof_sin_conversion + ad_concentracion_compras←meta_ads_performance por-ad (fecha BETWEEN, es_pagado); cvr/aov_fuera_de_banda←banda media±k·stddev_pop sobre `banda_ventana_semanas` snapshots previos; mix_canal_dominante←snapshot.mix_canal_web jsonb. Detectores por-ad exponen `ad_id` OPACO (extra key), NUNCA ad_name/campaign_name (vector injection F1-b).
- **`v_meta_ads_roas_real` NO tiene filtro de fecha** (agrega TODO el histórico por adset) → inútil para un detector parametrizado por semana; para grano semanal usar las columnas atribuidas del `weekly_snapshot` (roas_meta_atribuido = revenue_paid_atribuido/gasto_meta).
- **check-data-rules R1 es grep TONTO de `valor_compras`** sobre líneas AÑADIDAS: mencionar el token EN COMENTARIOS de una migración nueva lo dispara (FAIL, bloquea). Reformular sin el literal (p.ej. "auto-reporte del pixel de Meta"). `\broas\b` de R5 NO matchea `roas_meta`/`roas_margen_atribuido` (el `_` es word-char → sin boundary), así que columnas `roas_*` de weekly_snapshot son seguras.
- **`weekly_snapshot` NO tiene columnas GENERATED** (aov/cvr_web son normales ahí; el aov GENERATED es de `amplitude_daily_metrics`) → se pueden insertar en fixtures. `meta_ads_performance` GENERATED = ctr,cpc,roas,cpa (excluir de INSERT de fixtures); NOT NULL sólo id/fecha/ad_id.
- **Branch replay stale-abril (igual 234/235/236)**: falta `analytics`, `brand_config` (080), `set_updated_at`, y las 5 columnas margen/mix del weekly_snapshot (roas_meta_atribuido, revenue_paid_atribuido, mix_canal_web, roas_margen_atribuido, margen_paid_atribuido). Scaffolding branch-only recrea ese delta (verificado read-only vs PROD) antes de aplicar 134. Advisor `rls_disabled_in_public` sobre `brand_config` y `function_search_path_mutable` sobre `set_updated_at` en el branch = ARTEFACTOS del scaffolding (en PROD real ya están endurecidos por 080/053), NO del deliverable. Mis 2 funciones `analytics.*` NO aparecen en advisors (search_path fijado OK).
- **CA1 validado end-to-end** seedeando el branch con la semana real 2026-07-06 + los 6 ads reales: disparan klaviyo, margen_paid_negativo (impacto -126357), ad_tof (impacto -67632, reconciliado contra recompute crudo PROD por 2 formas), ad_concentracion (1 ad = 5/5 compras). mix/divergente/cvr/aov NO disparan esa semana (correcto). Selftest `analytics.evaluate_detectors_selftest()` .ok=true (subtransacción revertida, cero residuo, sin DELETE) consumido por `dashboard/evals/cerebro/detectors-eval.test.ts`.

## AIR-240 (Loop v3 F2-a) — trigger hecho→decisiones (mig 135)
- **Cuerpo del issue DESACTUALIZADO** (escrito pre-AIR-238): su INSERT de ejemplo rompe 3 CHECKs (`canal`=dominio crudo, `ejecutado_por`='humano_dashboard', cita `condicion_sql` que ya no existe). Usar el diseño corregido, no el literal.
- **`decisiones` CHECKs (PROD, verificados)**: `canal` ∈ {klaviyo,meta,shopify,pos,contenido,otro}; `ejecutado_por` ∈ {agente_auto,agente_aprobado,humano}; `resultado_evaluacion` ∈ {positivo,neutro,negativo}. NOT NULL: descripcion_accion, metrica_objetivo, valor_baseline, fecha_medicion. **`delta_real_pct` es GENERATED STORED** (excluir de INSERT). `marca_id` existe (nullable) — no se toca.
- **Mapeo dominio→canal usado**: meta_ads/paid→meta, email→klaviyo, web/ventas→shopify, organico→contenido, resto (producto/cliente/inventario/general)→otro. NUNCA insertar `dominio` crudo. `accion_tomada_por` (texto libre) NO cabe en `ejecutado_por` → va en `notas_resultado`; `ejecutado_por`:='humano'.
- **`insights` SÍ tenía 1 trigger antes**: `trg_insights_updated_at` (BEFORE UPDATE, mantiene updated_at). El nuevo `trg_insight_hecho_a_decision` (AFTER UPDATE→decisiones) es net-new; conviven. El WHEN `(OLD.estado_accion IS DISTINCT FROM 'hecho' AND NEW.estado_accion='hecho')` restringe el disparo; re-set a hecho cuando ya era hecho NO dispara (idempotencia a nivel trigger). La idempotencia REAL (pospuesto/en_curso→hecho) la cubre el guard interno `IF EXISTS(decisión abierta valor_resultado IS NULL)`.
- **Trigger fn puede llamar RPCs service_role-only**: SECURITY DEFINER owned by postgres (apply_migration corre como postgres → owner postgres automático, sin ALTER OWNER) → dentro del trigger current_user=postgres, que OWNS `evaluate_detectors` → EXECUTE implícito aunque el REVOKE/GRANT sea service_role-only. Re-declarar `SET search_path TO 'public','analytics'` en el CREATE (o reaparece function_search_path_mutable). El disparo del trigger NO chequea EXECUTE contra el rol del UPDATE → el REVOKE de la fn es solo defensa en profundidad, no rompe el fire.
- **Baseline del detector**: `evaluate_detectors(periodo_inicio,periodo_fin)` → extraer `valor` de la entrada con `insight_key=NEW.insight_key` (evaluate_detectors ya filtra `activo=true`). Guard: solo llamar si insight_key NOT NULL + período presente + detector activo existe (evita spam de log ai_analysis_log en cada hecho sin detector). Fallback: valor_observado + nota '...(sin detector)'; último recurso 0 (NOT NULL).
- **Branch replay stale-abril (igual 234/235/236/238)**: `insights` EXISTE pero SIN estado_accion/insight_key/accion_tomada_por (CREATE TABLE IF NOT EXISTS NO agrega cols → usar ALTER ADD COLUMN IF NOT EXISTS + DROP/ADD de los 3 CHECKs dominio/tipo/estado_accion). `weekly_snapshot`/`meta_ads_performance`/`ai_analysis_log` EXISTEN (weekly_snapshot le faltan las 5 cols atribuidas/mix; meta_ads_performance ya trae todas). `decisiones`/`insight_detectors`/`brand_config`/`analytics` NO existen → CREATE. Scaffolding branch-only recrea el delta + copia verbatim `evaluate_detectors` (mig 134). Selftest `.ok=true` (6 CA). Advisors: rls_disabled sobre insight_detectors/brand_config + set_updated_at mutable = ARTEFACTOS del scaffolding (PROD real endurecido por 134/080), NO del deliverable; mis 2 funciones `analytics.tg_*` y `decisiones` NO aparecen (RLS/search_path OK).

## AIR-257 (endurecer check-docstring-rpc-loop contra falso positivo por decimal narrativo)
- **`check-docstring-rpc-loop.sh` (AIR-135)**: extrae decimales del docstring-cabecera y verifica que cada "delta de score_confianza" aparezca en el cuerpo. Bug: el caso `sign=''` (factor) capturaba CUALQUIER decimal suelto → falso positivo con cita narrativa (`score 1.01` de Klaviyo en AIR-242). **Fix**: un decimal SOLO cuenta como delta si va precedido por operador de ajuste (`+`,`-`,`+=`,`-=`,`*`). Nuevo `sign='*'` para factor; `[ -z "$sign" ] && continue` descarta el decimal suelto (cita/asignación narrativa como `= 0.90`, `=1.01`, `(n=42)`). NO se debilita AIR-97 (deltas reales siempre traen operador).
- **Fixture AIR-97 REAL, permanente**: `supabase/migrations/033_analytics_close_insight_loop.sql` documenta en cabecera `contradice → score -= 0.15 (refutado)` pero el cuerpo solo hace `+0.10`/`-0.05` (nunca -0.15). El check FALLA sobre 033 vía `--file` (correcto: es el bug histórico). Como el check corre sobre `--diff origin/main` y 033 ya está en main, nunca bloquea; sirve de fixture real de "delta huérfano".
- **`AS $$` debe ir al inicio de línea** para que el check separe cabecera/cuerpo (regex `^[[:space:]]*AS[[:space:]]+\$\$`). Fixtures de test que pongan `RETURNS ... AS $$` en una sola línea dan WARN "no se halló AS $$", no comparan → cuidado al escribir fixtures.
- **Patrón selftest + pipefail**: con `set -o pipefail`, `run | grep -q` propaga el exit!=0 del LINT (que en casos de FAIL esperado sale 1) AUN si grep hace match → el `if` va al else. Para casos donde el LINT DEBE fallar, capturar salida a variable primero (`OUT="$(run ...)"; echo "$OUT" | grep`), como hace `check-data-rules.selftest.sh`. Inline pipe solo sirve cuando el LINT sale 0.
- **CI**: job `docstring-rpc-loop` en `.github/workflows/ci.yml` no tenía selftest; agregado step "Self-test del linter (AIR-257)" → `check-docstring-rpc-loop.selftest.sh` (6 casos: AIR-97 huérfano FAIL, narrativo PASS, deltas reales PASS, asignaciones narrativas PASS, exit codes).

## AIR-231 — REVOKE EXECUTE de RPCs SECDEF (regresión AIR-86)
- **El vector real es el grant a PUBLIC (`=X/postgres` en pg_proc.proacl), NO grants explícitos a anon/authenticated.** anon/authenticated normalmente NO aparecen como grantees explícitos: obtienen EXECUTE vía la membresía implícita en PUBLIC. Por eso `REVOKE ... FROM anon, authenticated` (patrón mig 060/AIR-86) es un **NO-OP** que deja los advisors `anon_/authenticated_security_definer_function_executable` en rojo. Hay que `REVOKE ... FROM PUBLIC, anon, authenticated`.
- Verificación read-only de ACL: `SELECT proname, pg_get_function_identity_arguments(oid), prosecdef, proacl::text FROM pg_proc JOIN pg_namespace ...`. `=X/postgres` = PUBLIC tiene EXECUTE. `has_function_privilege('anon', 'public.fn(types)', 'EXECUTE')` da la verdad efectiva por rol.
- Mig 060 (AIR-86) NO cubre funciones creadas/recreadas después: `notify_product_update()` (trigger fn nueva) y `analytics_aprobar_propuesta` (recreada en mig 101 AIR-162 → reset grant default a PUBLIC) reaparecen en advisor. Toda `CREATE FUNCTION` nueva o `CREATE OR REPLACE` que no preserve ACL re-abre el vector PUBLIC.
- `service_role=X` y `dashboard_reader=X` (los 3 `analytics_*`, grant AIR-82/84) son grants EXPLÍCITOS por rol → `REVOKE FROM PUBLIC/anon/authenticated` NO los toca. Es la garantía de que el HITL del dashboard sigue funcionando.
- **Preview branch INUTILIZABLE para verificar esto (bug replay confirmado 2026-07):** los 3 branches existentes están `MIGRATIONS_FAILED`, snapshot corta en **61 migraciones** (query directo `SELECT count(*) FROM supabase_migrations.schema_migrations` sobre el branch = 61) → las 6 funciones NO existen ahí (todas se definen/recrean en migraciones >61: 101, 105, etc.). Un branch fresco replay-ea el mismo historial y falla igual. Aplicar el REVOKE ahí ERRORA ("function does not exist" — REVOKE solo es idempotente para un GRANT ausente sobre un objeto EXISTENTE, no para un objeto ausente). Verificación se hace read-only contra PROD (proacl + has_function_privilege + get_advisors baseline).
- AIR-231 baseline advisor PROD = exactamente 12 hallazgos (6 fns × {anon, authenticated}_security_definer_function_executable). Migración: `142_air231_revoke_execute_secdef_rpcs.sql`.
